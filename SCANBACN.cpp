#include <bits/stdc++.h>
using namespace std;
int main() {
    int n,x;
    cin >> x >> n;
    while(x > 0)
    {
      double s = pow(x, 1.0/2);
      cout << fixed << setprecision(2) << s;
      break;
    }
}
